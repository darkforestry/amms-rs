[package]
name = "amms"
version = "0.7.0"
edition = "2021"
license = "MIT"
description = "A library to interact with automated market makers across EVM chains."
readme = "README.md"
homepage = "https://github.com/darkforestry/amms-rs"
repository = "https://github.com/darkforestry/amms-rs"
keywords = ["ethereum", "amm", "mev"]
exclude = ["target/*", ".github/*", ".gitignore"]


[dependencies]
# darkforest
uniswap_v3_math = "0.6"

# alloy
alloy = { version = "0.12.6", features = [
    "contract",
    "network",
    "rpc",
    "rpc-types",
    "provider-ws",
    "rpc-types-eth",
    "signer-local",
] }
alloy-json-rpc = { version = "0.12" }
alloy-provider = { version = "0.12.6", features = ["throttle"] }

# tracing
eyre = "0.6"
tracing = "0.1"

# async
tokio = { version = "1.42", default-features = false }
futures = "0.3"
future-utils = "0.12"
async-trait = "0.1"

# serde
serde = "1.0"
serde_json = "1.0"

# misc
arraydeque = "0.5"
lazy_static = "1.5"
num-bigfloat = "1.7"
regex = "1.10"
thiserror = "1.0"
rug = "1.24.1"
derive_more = "1.0.0"
governor = "0.7.0"
itertools = "0.13.0"
tower = "0.5.1"
rayon = "1.10.0"
async-stream = "0.3.6"


[dev-dependencies]
rand = "0.8.5"
tracing-subscriber = "0.3"
criterion = "0.5"
tokio = { version = "1.42", default-features = false, features = [
    "rt-multi-thread",
] }
alloy = { version = "0.12", features = ["rpc-client"] }

[build-dependencies]


[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

[profile.dev]
opt-level = 3
lto = true
codegen-units = 1
debug = "full"


[[bench]]
name = "uniswap_v2"
harness = false

[[bench]]
name = "uniswap_v3"
harness = false
