pub trait AutomatedMarketMaker {
    //address()
    //calculate_price
    // simulate_swap
    // stimulate_swap_mut
    // swap_calldata
    // tokens -> vec<token> (optional)
    // sync()
    // sync from log()
    // sync events (triggers to listen for that will cause to sync or something)
}

pub enum AMM {}
